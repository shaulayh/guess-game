<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head::headFragment}"
    />
    <attr sel="#titleId"
          th:text="#{game.title}"
    />
    <attr sel="#result-message"
          th:text="${resultMessage}"
    />

</thlogic>